services:
  mcp-app:
    build: .
    container_name: mcp-app
    restart: unless-stopped
    expose:
      - "8000"
    networks:
      - mcp-network
      - onyx_default

networks:
  mcp-network:
    driver: bridge
  onyx_default:
    external: true
